rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY DIAGNOSTIC RULE: Allow public read
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
